#!/bin/bash

git add -A current-id current v2-current-id v2-current firetext airborn strut &&
git commit -qm "Update airborn" &&
(
	git push -f origin master:deploy &&
	fly deploy
)
git reset -q HEAD^